import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.dreamyslothblink',
  appName: 'dreamy-sloth-blink',
  webDir: 'dist'
};

export default config;
